---
name: Prepare Release PR

# yamllint disable-line rule:truthy
on:
  # we don't want to trigger a release here as some setup is needed (done in init-workflow branch)
  workflow_dispatch:

jobs:
  default:
    # yamllint disable-line rule:line-length
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/terraform_module_release_manual_callable.yml@5ed58e34d3e7e9dcb3b416e01b8b1a65dbe2f10d
    secrets: inherit
