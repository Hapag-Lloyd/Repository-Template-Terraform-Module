---
name: Prepare Release PR

# yamllint disable-line rule:truthy
on:
  # we don't want to trigger a release here as some setup is needed (done in init-workflow branch)
  workflow_dispatch:

jobs:
  default:
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/release_manual.yml@c2e27d976e1221942c2186e512183ab79c1c8bac # 1.15.4
    secrets: inherit
