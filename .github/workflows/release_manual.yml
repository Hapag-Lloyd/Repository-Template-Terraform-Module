---
name: Prepare Release PR

# yamllint disable-line rule:truthy
on:
  # we don't want to trigger a release here as some setup is needed (done in init-workflow branch)
  workflow_dispatch:

jobs:
  default:
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/release_manual.yml@6f2cce3f474216dd3b5ee5b820eb8ef6acfcfbe7 # 1.15.4
    secrets: inherit
