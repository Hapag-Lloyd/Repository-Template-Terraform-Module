---
name: Release

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  default:
    # using a GitHub only release here. Use .github/update_workflows.sh to switch to a Docker release
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/default_release_callable.yml@733afe1105fc85fafc25cddbc3c34b48c2c3d6fa
    secrets: inherit
