---
name: TfSec

# yamllint disable-line rule:truthy
on:
  pull_request:
  # runs mondays at 3:47 UTC refreshing the vulnerabilities
  schedule:
    - cron: "47 3 * * 1"

jobs:
  default:
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/terraform_tfsec.yml@e8c1c3deb93315b82c4bcd4957b215fd5f94e1c2 # 1.6.3
    secrets: inherit
